#include <math.h>

float bl3d_sin(const float a)
{
	return sinf(a);
}

float bl3d_cos(const float a)
{
	return cosf(a);
}

float bl3d_sqrt(const float a)
{
	return sqrtf(a);
}

float bl3d_atan2(const float a, const float b)
{
	return atan2f(a, b);
}
