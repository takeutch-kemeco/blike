SRCS =
OBJS = $(SRCS:.c=.o)
PROG =

CFLAGS = @CFLAGS@ `pkg-config c_blike --cflags`
LIBS = @LIBS@ `pkg-config c_blike --libs`

all:
	@CC@ @DEFS@ $(CFLAGS) -o $(PROG) $(SRCS) $(LIBS)

clean:
	rm -f $(OBJS)
	rm -f $(PROG)

