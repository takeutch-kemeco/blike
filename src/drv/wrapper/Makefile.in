INC_DIR = @top_srcdir@/include

SUBNAME = wrapper
PROG = @PACKAGE_NAME@_$(SUBNAME).a
CFLAGS = @CFLAGS@
LIBS = @LIBS@

SRCS	= bl_main_wrapper.c
OBJS	= $(SRCS:.c=.o)

all: $(PROG)

$(PROG): $(OBJS)
	ar -rv $@ $(OBJS)

.c.o:
	@CC@ $(DEFS) $(CFLAGS) -c -o $@ $<

clean:
	rm -f $(OBJS)
	rm -f $(PROG)
